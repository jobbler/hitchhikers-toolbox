---
description: compare two yaml files
languages: bash
---

# yaml-compare

This script is used to compare two yaml files. It may not be perfect, but it helps a lot.

It does this by first normalizing the files by sorting its keys. It then simple performs a diff on the files.

Use the `--help` option to show how to use the script.

